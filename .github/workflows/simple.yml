name: shell Commands

on: [push]

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: eccho "Hello world"
      - name: multiline Scritt
        run: |
          node -v
          npm -v